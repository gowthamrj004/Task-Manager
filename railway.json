{
  "buildCommand": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install && VITE_API_URL=/api npm run build",
  "startCommand": "cd backend && npm run start:with-migrations"
}
